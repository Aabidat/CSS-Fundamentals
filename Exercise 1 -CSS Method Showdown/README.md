# CSS Method Showdown
## Exercise-1
1. The paragraph currently displays orange, because the method used was inline CSS and this method overrides both internal and external CSS because it directly targets the element.
2. I added the line of code to an external CSS file and it overrided the inline CSS because I used the !important declaration to increase the specificity of the external CSS.
3. It is not possible because inline CSS overrides internal CSS and this is because inline CSS directly targets the element, so the color still remains orange.